App As appinfo:
    BackEnabled: =true
    Formulas: |-
        =tabListItems = Table({ name: "Zeiterfassung", Screen: Zeiterfassung},{ name: "Report", Screen: Report});
    OnStart: =Set(varDPStop, DisplayMode.Disabled);Set(varDPBuchen,DisplayMode.Disabled)
    SizeBreakpoints: =[600. 900. 1200]
    Theme: =Yaveon

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

